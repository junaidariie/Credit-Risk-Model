name: fastapi-app
language: python
python_version: "3.11"

build:
  commands:
    - pip install -r requirements.txt

run:
  command: uvicorn app:app --host 0.0.0.0 --port $PORT
  port: $PORT

health_check:
  path: /
